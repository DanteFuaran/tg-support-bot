# 🤖 Telegram Support Bot
Система поддержки на базе Telegram-бота.
___
## Бот поддерживает:

+ 🎟️ автоматическое создание тикетов для каждого обращения

+ 💬 удобное общение с пользователями через темы в группе

+ 🔔 уведомления о новых сообщениях

+ ⏳ автозакрытие неактивных тикетов
___

## 🚀 Установка за 1 минуту

```bash
# Полная установка одной командой

wget https://raw.githubusercontent.com/DanteFuaran/tg-support-bot/master/install.sh -O install.sh && chmod +x install.sh && sudo ./install.sh && rm install.sh
```

### 📦 Что делает скрипт:

+ Обновит систему и установит все зависимости

+ Клонирует репозиторий

+ Создаст и настроит виртуальное окружение

+ Установит зависимости Python

+ Настроит автозапуск

+ Установит панель управления

### ⚙️ Что нужно подготовить заранее

+ 🔐 Токен Telegram-бота (получить у @BotFather)

+ 👥 ID группы поддержки (в формате -1001234567890)

___

## 🧰 Альтернативная ручная установка

Если хотите установить вручную — пошагово:

```bash
# Подготовка системы

apt update && apt upgrade -y
apt install python3 python3-pip python3-venv git -y
```
<br>

```bash
# Клонирование репозитория

cd /opt
git clone https://github.com/DanteFuaran/tg-support-bot.git
cd tg-support-bot
```
<br>

```bash
# Создание окружения

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
deactivate
```
<br>

```bash
# Настройка окружения

cp .env.example .env
nano .env  # Укажите токен и ID группы
cp config.example.py config.py
cp storage.example.json storage.json
```
<br>

```bash
# Настройка автозапуска

cp tg-support-bot.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable tg-support-bot.service
systemctl start tg-support-bot.service
```
___

## 🔧 Если бот не запускается

+ Убедитесь, что `BOT_TOKEN` и `SUPPORT_GROUP_ID` заполнены в .env

+ Убедитесь, что виртуальное окружение `.venv` создано

___

❤️ Благодарности

Бот создавался с упором на простоту, стабильность и автоматизацию.<br>
Спасибо всем, кто помогает тестировать и улучшать систему.
